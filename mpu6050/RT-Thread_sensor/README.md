RT-Thread/sensor是RT-Thread针对一些传感器的专门分支版本，和开发主干、分支主要体现在以下的不同上：
1. 占用资源低，包括ROM及SRAM的资源占用上；所以这部分也会包括一些RT-Thread的深度裁剪；
2. 面向多种传感器设备，例如一些IIC、GPIO、SPI的传感器设备，这个主要是使用RT-Thread的抽象IIC、SPI总线接口来编写与底层硬件无关的传感器设备驱动；
3. 低功耗，包括低功耗用到的一些低功耗定时器（也会把低功耗定时器抽象成传感器设备）。
4. 面向的硬件平台主要是STM32F401 discovery开发板，后期会考虑资源更小型的芯片平台；
